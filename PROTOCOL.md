# PROTOCOL.md — پروتکل همکاری چند‌عاملی روی این ریپو

**قانون اول:** این ریپو = منبع واحد حقیقت. هر تصمیم، کار و تحویل باید **در این ریپو نوشته باشد**؛ قول کلامی (در هر چتی) اعتبار ندارد.

## نقش‌ها

| نقش | چه کسی | اختیارات |
|---|---|---|
| **مالک (انسان)** | صاحب حساب | مرجع نهایی؛ مِرج نهایی به `main` با تأیید اوست (یا به تفویض صریحش) |
| **AI-A (عامل اجرا)** | عامل فعلی | تمام `Contents` + `Pages` |
| **AI-B (عامل دوم، در صورت پیوستن)** | هر AI دیگری که مالک دسترسی بدهد | ترجیحاً همان دو دسترسی؛ اگر اعتماد کمتر است: فقط Contents + بازکردن PR |

## کانال ارتباط (اینترنت بین دو AI وجود ندارد — ریپو تخته‌ی مشترک است)

- **کار = issue:** برچسب‌ها: `بحرانی` / `مهم` / `خوب‌داشتن` + حرف ماژول سرلوحه (A تا M).
- **اجرا = شاخه:** `ai-a/task-<شماره-issue>-<slug>` و `ai-b/...`
- **تحویل = PR:** در بدنه‌ی PR حتماً: چه شد / چه تست شد / چه چیزی منتظر بازبینی است.
- **مشاوره = کامنت issue/PR:** اختلاف‌نظر کتبی ثبت می‌شود؛ اگر دو AI به توافق نرسیدند، issue برچسب `تصمیم-مالک` می‌گیرد و دست مالک است.
- **حافظه‌ی مشترک** (هر AI جدید **قبل از هر کاری** این سه فایل را می‌خواند):
  1. `PROTOCOL.md` (این فایل)
  2. `docs/SAYA-Prompt-v1.2.md` (سرلوحه)
  3. `docs/SAYA-Review-1405.md` (تحقیقات و شگردها)

## قواعد غیرقابل‌مذاکره (از اصول قفل سرلوحه)

1. **هیچ رمز/کلید/توکنی** وارد ریپو نشود — نه در فایل، نه در کامیت، نه در تاریخچه.
2. **force-push به main ممنوع**؛ حذف ریپو و بازنویسی تاریخچه ممنوع.
3. **داده‌ی فیک ممنوع** در دمو/سایت (اصل ۳ سرلوحه).
4. هر تغییری که با سرلوحه تعارض دارد: **issue باز شود، نه تغییر**.
5. هر AI فقط روی issue‌هایی کار کند که در PR قبلی‌اش claim کرده باشد (برچسب `در-ادامه-<نام-agent>`) — تا دو عامل روی یک فایل پا روی هم نیندازند.

## روال هر دور (هر دو AI دقیقاً همین را اجرا کنند)

1. `PROTOCOL.md` + سرلوحه را بخوان.
2. `git log` آخر + issues باز را ببین (از API: `GET /repos/.../issues?state=open`).
3. بحرانی‌ترین issue بدون claim را claim کن (برچسب بزن).
4. شاخه بزن، کار کن، **تست بزن**، PR باز کن.
5. اگر PR دیگری باز است و مال تو نیست: اگر توانی بازبینی/کامنت کن؛ در غیر این صورت سراغ issue بعدی برو.

## امنیت دسترسی

- هر AI **توکن separate خودش** را دارد (fine-grained، فقط این ریپو). نشت توکن یکی → فقط همان Revoke می‌شود.
- توکن‌ها فقط در لحظۀ اجرا استفاده می‌شوند؛ در هیچ فایلی ذخیره نمی‌شوند.
- مالک هر لحظه از Settings → Developer settings → Fine-grained tokens می‌تواند هر کدام را ببیند/ببندد.
