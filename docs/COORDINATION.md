# هماهنگی AI-A / AI-B

آخرین به‌روزرسانی: ۲۴ شهریور ۱۴۰۵ (۲۰۶-۰۹-۱۵) — توسط AI-A (بعد از یکپارچه‌سازی با AI-B)

## 💬 بحث مستقیم
توکن‌ها اجازه‌ی کامنت روی issue ندارند؛ بحث کتبی دو AI در **`docs/DISCUSSION.md`**
ثبت می‌شود (append-only). آخرین بحث: یکپارچه‌سازی دروازه‌ی مالی + ۳ سؤال برای AI-B.

## قواعد
1. هر AI روی شاخه‌ی جدا می‌نویسد: `ai-a/*` و `ai-b/*`.
2. merge فقط توسط مالک (owner) انجام می‌شود.
3. کارِ «ادعاشده» توسط دیگری را تکرار نکن؛ اگر مرز تداخل دارد، در همین فایل ثبت کن.
4. هر تغییر: selftest سبز + commit message فارسی.

## ⭐ شاخه‌ی پیشنهادی برای merge: `ai-a/integrated`
**همه‌ی کارِ هر دو AI در یک شاخه، یک‌جا تست‌شده — selftest 59/59.**
- کار AI-A: تقویم دقیق، تأیید فاکتور، بازتلاش یادآور، گزارش صبح محلی، آستانه‌ی ۵۰٪
- کار AI-B: دروازه‌ی دسترسی (MANAGER_ONLY_CMDS) + `.gitignore` + ۷ تست
- مالک فقط این شاخه را merge کند؛ دو شاخه‌ی قبلی دیگر لازم نیستند.

## وضعیت شاخه‌ها

### `ai-a/integrated` (AI-A) — پیشنهاد merge ⭐
یکپارچه‌سازی ۴ فایل hardening + ۵ تغییر AI-B؛ 59/59.

### `ai-b/task-2-finance-gate` (AI-B) — بایگانی
- دروازه‌ی FINANCE: دستورات مالی (`/credit` و مشابه) فقط از مالک اجرا می‌شود (MANAGER_ONLY_CMDS).
- وضعیت: **کارش وارد `ai-a/integrated` شد**؛ شاخه برای سوابق می‌ماند.

### `ai-a/hardening` (AI-A) — بایگانی
تغییرات (۴ فایل: `bot/persian.py`, `bot/store.py`, `bot/talk.py`, `bot/bot.py`)، selftest ۵۲/۵۲:

1. **تقویم جلالی دقیق** (Borkowski 1996، پورت از jalaali-js): `j2g`/`g2j`/`is_leap_jy` + `parse_jalali` با کف‌شکن کبیسه. مدل قدیمی ۳۳-ساله (که اسفند ۱۳۹۹ را ۲۹ روز می‌دانست) حذف شد. ۱۰٬۰۰۰ round-trip سبز.
2. **تأیید فاکتور** (issue #5): عکس → پیش‌نمایش با اطمینان per-field (🟢/🟡) → «بله» → انبار؛ «نه» → لغو. **هیچ‌وقت ثبت خودکار نیست** (اصل ۲ سرلوحه). مشتری: عکس یا «مبلغ:» فقط برای صاحب مغازه ارسال می‌شود.
3. **بازتلاش یادآور**: تا ۳ تلاش؛ اگر ۳ بار شکست → mark + log `reminder-failed` (داده پاک نمی‌شود تا مالک ببیند).
4. **گزارش صبح**: ساعت ۹ به بعد به‌وقت محلی + catch-up (اگر بوت بعد از ۹ بالا آمد، همان روز می‌فرستد؛ روزی یک‌بار).
5. **آستانه‌ی ۵۰٪** (سرلوحه): `/credit` بدون تأیید فقط تا نیمه‌ی سقف.

## ✅ تداخل `/credit` — حل شد (در `ai-a/integrated`)
- **AI-B**: دروازه‌ی «کی» می‌تواند اجرا کند (فقط مالک) — گیت ابتدای `_handle_text`.
- **AI-A**: دروازه‌ی «چقدر» (بالاتر از ۵۰٪ سقف → approval) — داخل `_cmd_credit`.
- هر دو مکمل‌اند؛ ترتیب در کد: اول گیت (بلاک مشتری)، بعد آستانه (بر مالک). تست «customer blocked: /credit» این را قفل می‌کند.
- نکته‌ی UX: `«مبلغ: عدد»` از مشتری **بلاک نمی‌شود** — آن درخواست ثبت است، نه دستور مالی؛ برای صاحب مغازه ارسال می‌شود (اصل ۲).

## فهرست issueهای باز که هر دو می‌بینیم
- #5 تأیید فاکتور — **AI-A انجام داد** (این شاخه).
- مالی/دروازه — **AI-B انجام می‌دهد**.
- بقیه: در `issues` repo.
