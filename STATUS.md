# وضعیت — ۲۶ شهریور ۱۴۰ (برای مالک)

## ⭐ ۲۶ شهریور — تریال ۱۰ روزه + بسته‌ها + باتری + ریکاوری (دستور شما) + ربات زنده

**ربات:** با توکن جدید **@sayajadubot** (ID 8983796452) برای تست زنده است؛ مدیر = شما (7106543460).
اولین قدم شما: در تلگرام @sayajadubot را باز کنید و **Start** بزنید (قاعده‌ی تلگرام: ربات نمی‌تواند اولین پیام را خودش بفرستد).

**سیستمِ جدید — پیاده‌سازی و تست شد (دستور ۲۶ شهریور):**
1. **تریال ۱۰ روزه** (تبلیغ): مشتری /start می‌زند → ۷ سؤال (نام، مغازه، صنف، شهر، موبایل، مشتری، قلم) → «🔍 در حال بررسی اطلاعات» → «✅ X٪ اطلاعات واردشده صحیح است» → تریال با باتری ۱۰۰٪ باز می‌شود؛ شما هم‌زمان اعلان می‌گیرید.
2. **بسته‌ها (ماهانه، تومان):** شروع **۳۳۰٬۰۰۰** (۱۰ مشتری/۵۰ قلم/۳۰۰ گفتگو/۶۰ دقیقه) · مغازه **۹۹۰٬۰۰۰** (۳۰/۲۰۰/۱٬۸۰۰/۱۲۰) · بازار **۱٬۹۰۰٬۰۰۰** (۱۰۰/۱٬۰۰۰/۴٬۸۰۰/۳۰۰).
3. **حاشیه‌ی سود ≥ ۳۰٪:** حتی در بدترین فرض (همه‌ی گفتگوها LLM) حاشیه‌ها ۸۰/۶۴/۵۱٪ — با assert در selftest.
4. **نرخ اضافه‌ها:** هر ۱۰ مشتری ۷۵٬۰۰۰ · هر ۵۰ قلم ۵۰٬۰۰۰ · هر ۱۰۰ گفتگو ۱۰۰٬۰۰۰ · هر ۳۰ فاکتور ۱۵٬۰۰۰ · هر ساعت تماس/جواب‌گویی **۹٬۰۰۰**.
5. **باتری نمادی** (مثل موبایل): گفتگو=۱ · فاکتور=۰.۵ · دقیقه صدا=۰.۱۵ واحد؛ `/battery`؛ هشدار <۲۰٪؛ خالی‌شدن = «رسید بفرست».
6. **پس از موعد:** روز ۱–۷ باتری چشمک‌زن + «اطلاعاتِ شما حذف خواهد شد» (روزی یک‌بار) + اعلان به شما؛ روز ۸+ **یخ‌زدگی** (داده در سرور می‌ماند) + **حق‌البازگشت ۳٬۰۰۰/روز**.
7. **مسیر بازگشت:** مشتری عکسِ رسید → ربات → شما → `/charge نام` (فعال + ۳۰ روز) یا `/recover نام` (با حق‌البازگشت، تأییدِ دوم). مدیریت مالی/رفتاری: `/manage` `/freeze` `/thaw` `/note` `/plan`.

**تست‌ها:** selftest ربات **۷۷ ← ۱۰۶/۱۰۶ سبز** · unit-econ **v2.2 سبز** (تریال ۱۰ روز + assert حاشیه) · سایت **۳۰/۳۰ سبز**.
**سایت:** «۷ روز آزمایش» → «۱۰ روز آزمایش» (۳ نقطه) + همه‌ی ۷ موردِ round-2 قبلی زنده است.
**باگ‌بندی:** selftest وابسته به ساعت (گزارش صبح بعد از ۹ صبح زودتر می‌افتاد) اصلاح شد؛ گزارش صبح فقط بعد از ارسال موفق «فرستاده شد» می‌خورد.
**سند کامل:** `docs/PRICING.md`
**باقی‌مانده:** نمایشِ بسته‌ها در سایت · درگاه پرداخت · OCR روی VPS · deploy پایدار روی VPS.



## ✅ ۲۵ شهریور — merge انجام شد
- `ai-a/final` (۴۸ commit) با PR #۱۴ به main مردج شد (commit 7283ddbb).
- سایت زنده بررسی شد: «هر ماه» + «دوره‌ی پرداخت: ماهانه» + خط «گفتگوی هوشمند اضافی: ۱٬۰۰ تومان» روی هر سه کارت.
- خودتست اقتصاد واحد روی main: سبز.
- باقی‌مانده: بستن issueهای #۱–#۷ و #۱۰ (اختیاری، توسط مالک) · token بات + «اجرا» · قفل main (اختیاری).

## ⭐ ۲۵ شهریور — اقتصاد واحد v2.1 (واقع‌بینانه) — برای تصمیم‌گیری

به دستور شما («دقیق‌تر و واقعی‌تر») مدل اقتصاد واحد را از اعداد سرخود به **استک هزینه/درآمد کاملاً نام‌گذاری‌شده** ارتقا دادم. همه‌چیز در `docs/unit-econ/` و **تست‌شده** است (`python3 unit_econ.py --selftest`)؛ اینفوگرافیکش (`index.html`) هم حالا کد-ژنره‌د است — هیچ عددی دست‌نویس نیست.

**سه یافته‌ی اصلی:**
1. **چرخه‌ی ۹۰روزه (فصلی) = هرگز سربه‌سر نمی‌شود** — حتی با رشد ۲۵۰ مغازه/ماه. با چرخه‌ی ۳۰روزه + تیم: رشد ۱۲۰/ماه → سربه‌سر **ماه ۱۶** (سوخت ≈ ۶.۵ میلیارد). حداقل رشد ممکن: ۱۱۰ مغازه/ماه (بدون بافر).
2. **سقف آنبردینگ، قفل واقعی است:** آنبردینگ تلفنی هر مغازه ≈ ۲ ساعت است؛ یک نفر ≈ ۳۰ مغازه/ماه. مسیر «تنها مالک» با هزینه‌ی ثابت ۹۵۰ میلیون **هرگز** سربه‌سر نمی‌شود (سوخت ۱۴.۴ میلیارد) — یا تیم کوچک آنبردینگ/فروش لازم است، یا تیم لاغر (ثابت ≈۴۵۰ میلیون) با رشد ۶۰+/ماه (سربه‌سر ماه ۱۰).
3. **باتری حالا خط درآمد واقعی است** (حاشیه ۸۹–۹۰٪) و منشی هیبرید (٪80 قاعده‌ای + ٪20 LLM ارزان ≈ ۳۸ تومان/گفتگو) حاشیه‌ی پلن‌ها را ۹۰٪+ نگه می‌دارد.

**تصمیم‌ها — گرفته شدند (۲۵ شهریور) و اعمال:**
- ✅ **چرخه‌ی صورتحساب: ۳۰ روز** — بنیاد مدل شد؛ چرخه‌ی ۹۰روزه سناریوی ردشده.
- ✅ **قیمت گفتگوی باتری: ۱۰۰۰ تومان** — تأیید شد.
- ✅ **پچ سایت (فقط کپی، بدون JS):** «هر فصل» → «هر ماه» (۹ نقطه) + خط «گفتگوی هوشمند اضافی: ۱۰۰۰ تومان» روی هر سه کارت. تست سایت **۲۹/۲۹ سبز**. روی `ai-a/final` — **زنده با merge شما به main** (بعد از merge، سایتِ Pages خودش آپدیت می‌شود).

جزئیات کامل فرض‌ها و اعداد: `docs/unit-econ/index.html` و `docs/DISCUSSION.md` (entry آخر). AI-B دعوت شده هر فرض را چالش کند؛ اگر اختلافی ماند ⚠️تصمیم-مالک + ادامه‌ی کار هر دو.

## ⭐ خلاصه‌ی این دور: هاردنینگ + یکپارچه‌سازی با AI-B — `ai-a/integrated`

ربات را از «MVP درست» به «قوی و ضدشکست» رساندم و **کار هر دو AI را در یک شاخه یک‌جا تست کردم**:
selftest: **۳۶ ← 77/77** (کار هر دو AI در یک شاخه، همه سبز).

**چرا این مهم است:** شاخه‌ی AI-B اول روی بازلن قدیمی ساخته شده بود و دو شاخه خط‌به‌خط روی `_handle_text` روی هم می‌افتادند. حالا:
۱. کارِ هر دو در **یک شاخه** است؛ ۲. AI-B کل کار من را بازبینی کرد و **دو باگ واقعی** پیدا کرد (باگ «22/7 ← ۲۲۷ دقیقه» را حتی تست خودم نمی‌دید!) و پچ داد؛ ۳. من پچ او را مستقل اجرا و تایید کردم (72/72). نتیجه: یک شاخه‌ی نهایی که **باگ‌های هر دو طرف** در آن بسته است.

### ۱. تقویم جلالی — دقیق شد (بحرانی)
مدل قدیمی ۳۳-ساله که **اسفند ۱۳۹۹ را ۲۹ روز** می‌دانست (کف‌شکن کبیسه را ندارد) با الگوریتم دقیق **Borkowski 1996** (همان jalaali-js) جایگزین شد. حالا:
- کف‌شکن کبیسه درست: `اسفند ۳۰/۱۳۹۹ = 2021-03-20` (قبلش خطا می‌داد)
- ۱۰٬۰۰۰ round-trip میلادی⇄شمسی سبز
- تاریخ‌های `/remind` و سررسید `/credit` حالا شمسی هم می‌گیرند (مثلاً `22/7`)

### ۲. تأیید فاکتور — اصل ۲ سرلوحه (بحرانی)
قبل: عکس → ثبت خودکار در انبار. بعد:
- عکس → پیش‌نمایش با اطمینان **per-field** (🟢 درست / 🟡 مشکوک) → مالک «بله» → انبار؛ «نه» → لغو. **هیچ‌وقت خودکار نیست.**
- مشتری (حتی اگر عکس یا «مبلغ:» بفرستد) → فقط برای صاحب مغازه ارسال می‌شود، ثبتی نمی‌شود.

### ۳. یادآور — ضدشکست
اگر ارسال صدا/متن شکست، **تا ۳ بار** تکرار می‌شود؛ اگر ۳ بار هم نشد، mark می‌شود + `reminder-failed` در log (داده پاک نمی‌شود).

### ۴. گزارش صبح — پنجره‌ی محلی + catch-up
ساعت ۹ به بعد به‌وقت محلی؛ اگر بوت بعد از ۹ بالا آمد، همان روز جبران می‌شود (روزی یک‌بار).

### ۵. آستانه‌ی ۵۰٪ اعتبار (سرلوحه)
`/credit` بدون تأیید، فقط تا **نیمه‌ی سقف**. بالاتر از آن → approval مالک.

## هماهنگی با AI-B
- **کانال بحث:** `docs/DISCUSSION.md` (تخته‌ی مشترک کتبی — توکن‌ها اجازه‌ی کامنت روی issue ندارند).
- ✅ **تداخل `/credit` حل شد:** گیت «کی» (AI-B) + آستانه «چقدر» (من)، هر دو در final، با تست قفل‌شده.
- ✅ **فز ۱b — مسیر صوت کامل شد:** فرمان با صدا (STT: **Vosk محلی آفلاین** یا `STT_URL`) + وقتی STT نبود، **صدای مشتری برای صاحب‌مغازه ارسال می‌شود** (قبلاً گم می‌شد) — ۵ تست جدید.
- ✅ **دو باگ تاریخ شمسی** که AI-B در بازبینی پیدا کرد، پچ و تایید شد: (۱) `/credit` با تاریخ نامعتبر دیگر بی‌صدا +۳۰ روز نمی‌شود — رد صریح دارد؛ (۲) «22/7» دیگر به ۲۲۷ دقیقه تبدیل نمی‌شود — اول تاریخ، بعد دقیقه. به‌علاوه فرمت ISO شمسی «۱۴۰۵-۰۷-۲۵» هم الان کار می‌کند.
- **پروتکل رسمی تو (ثبت شد برای هر دو AI):** AI-B شاخه را سرچ/بازبینی کند ← ایراد دید، اعلام کند ← با هم به نتیجه برسیم ← اگر نشد: `⚠️ تصمیم-مالک` + هر دو موضع؛ **هر دو به کارمان ادامه می‌دهیم** تا جواب تو.
- ✅ **issue #7 (بهداشت کد) کامل شد:** dead code، «٬» (قبلاً حل بود)، پرش ۳۰-روزه‌ی log با تست، مکث ۳ ثانیه‌ای حلقه بعد از خطای ماندگار.

## وضعیت شاخه‌ها
| شاخه | چه | وضعیت |
|---|---|---|
| `main` | سایت + MVP | زنده (سایت) |
| ⭐ `ai-a/final` | کار هر دو AI + بازبینی متقابل + ۲ باگ بسته‌شده + مسیر صوت | **آماده merge — 77/77** |
| `ai-b/task-5-on-integrated` | (لایه‌ی AI-B — داخل final است) | — |
| بقیه‌ی شاخه‌ها | بایگانی | — |

## قانون‌های سخت (از مالک — رعایت می‌شود)
1. **تا نگفته باشی، به هیچ‌کس پیام نمی‌دهم** — ربات بدون token + «اجرا» فعال نمی‌شود.
2. **هیچ اقدام جذب مشتری** تا اتمام پیش‌نیازها — GTM-LAUNCH فقط سند تا «شروع».

## ⭐ جدید: مدل اقتصاد واحد v2 (docs/unit-econ) — یک تصمیم از تو
اینفوگرافیک v1 (خارجی) را بررسی کردم و **مدل محاسباتی مرجع تست‌شده** ساختیم: `docs/unit-econ/unit_econ.py` + اینفوگرافیک v2.
**یافته‌ی حیاتی:** متن سایت هم «۹۹ هر فصل» و هم «۳۳ هزار در روز» می‌گوید — این دو با ۹۰ روز سازگار نیست (۳۳k×۹۰=۲٬۹۷۰٬۰۰۰T). یعنی یا دوره **۳۰ روزه** است یا جمله‌ی روزانه غلط.
اثرش: با دوره‌ی ۹۰ روزه، هیچ سناریویی در ۱۸ ماه به سربه‌سر نمی‌رسد؛ با ۳۰ روزه، سناریوی میانه ماه ۱۲ می‌رسد (سوخت ~۳٬۷ میلیارد).
**تصمیم مالک: دوره‌ی صورتحساب ۳۰ روزه است یا ۹۰ روزه؟** (تا آن‌وقت سایت باید یکی از این دو جمله را اصلاح کند.)

## منتظر توست
| # | کار |
|---|---|
| 1 | token ربات از @BotFather + «اجرا» (مراحل در `bot/README.md`) |
| 2 | merge **یک** شاخه: `ai-a/final` ← `main` |
| 3 | بعد از merge، این issueها بسته می‌شوند: #1 (حذف)، #2 تا #7، #10 — توکن‌های ما اجازه‌ی close issue ندارند |
| 5 | (اختیاری) repo خصوصی `saya-doks` برای ۲ سند راهبرد |
| 6 | اگر AI-B در بازبینی ایراد زد و با هم جمع نشدیم: `⚠️ تصمیم-مالک` در `docs/DISCUSSION.md` می‌بینی — تا آن وقت هر دو ادامه می‌دهیم |
